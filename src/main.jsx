import React from "react";
import ReactDOM from "react-dom/client";
import HealthStore from "./HealthStore.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HealthStore />
  </React.StrictMode>
);
